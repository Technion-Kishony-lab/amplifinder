classdef directed_IS < IS_super
    % Represents an IS with specified ID and ORIENTATION
    
    properties (Constant = true, Hidden = true)
        POSTFIX = ["R", "u", "F"]  % Reverese, unknown, Forward
    end
end
